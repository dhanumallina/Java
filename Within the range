                            < WITHIN THE RANGE >
/*You are given a positive number n, you need to return True if it is within 3 of a multiple of 10.
The multiples of 10 are 0, 10, 20, 30, 40...and so on. So, within 3 of 0 means -3 to 3, and within 3 of 10 means 7 to 13 and similarly for the others.*/

Class Solution 
{
public static String withinTheRange (int n) 
{
int rem=n%10;
if ( rem <= 3 || rem>=7)
{
return "True";
}
else
{
return "False";
}
}
